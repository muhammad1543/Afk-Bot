name: files
description: Organize, inspect and generate files inside the configured agent workspace.
permissions: filesystem_workspace

## Workflow
Use only the configured workspace. Keep destructive operations approval-gated. Preserve originals when transforming user files.
